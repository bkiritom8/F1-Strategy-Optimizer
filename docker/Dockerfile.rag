FROM python:3.10-slim

WORKDIR /app

COPY docker/requirements-rag.txt .
RUN pip install --no-cache-dir -r requirements-rag.txt

COPY rag/ ./rag/
COPY src/ ./src/

ENV PYTHONPATH=/app

CMD ["python", "-m", "rag.ingestion_job"]
